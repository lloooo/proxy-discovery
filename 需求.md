# Windows 局域网代理自动发现与网卡切换工具

## 一、项目目标

开发一个 Windows 桌面小工具，用于：

1. 在有线网卡和无线网卡之间切换
2. 自动扫描当前局域网
3. 自动发现 `7890` 和 `1082` 端口
4. **只通过 TCP 端口连通性判断代理是否存在**
5. 找到可连接的 IP:Port 后，自动设置 Windows 系统代理
6. 持续监控网络和代理端口状态
7. 代理失效后自动重新扫描

主要使用场景：

### 场景 A：iPhone 热点

Windows 通过 Wi-Fi 连接 iPhone 个人热点。

iPhone 上的 Shadowrocket 提供 HTTP 代理：

```text
7890
1082
```

程序自动找到 iPhone IP，例如：

```text
172.20.10.1:7890
```

然后设置 Windows 系统代理。

### 场景 B：电脑 A/B 网线直连

例如：

```text
电脑 A：192.168.10.10
电脑 B：192.168.10.20
```

电脑 B 提供 HTTP 代理：

```text
192.168.10.20:7890
```

电脑 A 自动扫描：

```text
192.168.10.0/24
```

找到：

```text
192.168.10.20:7890
```

并设置 Windows 系统代理。

------

# 二、重要约束

## 1. 代理端口

默认扫描：

```text
7890
1082
```

两个端口都视为 **HTTP Proxy**。

但是：

> 程序不需要验证 HTTP Proxy 协议，也不需要验证代理能否访问互联网。

只要 TCP 连接成功，就认为该 IP:Port 是可用代理。

------

# 三、代理检测规则

这是本项目非常重要的简化要求。

### 只进行 TCP Connect

例如：

```text
192.168.10.20:7890
```

程序执行 TCP connect。

如果：

```text
TCP连接成功
```

则：

```text
认为代理可用
```

如果：

```text
连接超时
Connection refused
Network unreachable
其他 TCP 连接错误
```

则认为：

```text
代理不可用
```

### 禁止做以下事情

不要：

- 发送 HTTP GET
- 发送 CONNECT
- 请求任何 URL
- 验证 HTTP 状态码
- 验证代理能否访问互联网
- 验证代理节点是否正常
- 验证 DNS
- 验证 HTTPS
- 验证公网 IP

本工具只关心：

> **IP:Port 是否能够建立 TCP 连接。**

------

# 四、扫描逻辑

获取当前启用网卡的：

- IPv4
- 子网掩码
- CIDR

例如：

```text
IP：192.168.10.10
Mask：255.255.255.0
```

得到：

```text
192.168.10.0/24
```

然后扫描：

```text
192.168.10.1:7890
192.168.10.1:1082

192.168.10.2:7890
192.168.10.2:1082

...

192.168.10.254:7890
192.168.10.254:1082
```

使用并发扫描。

建议使用：

```python
concurrent.futures.ThreadPoolExecutor
```

并配置：

```json
{
  "scanTimeout": 500,
  "scanConcurrency": 100
}
```

------

# 五、iPhone 热点优化

如果当前网卡存在默认网关：

例如：

```text
IP：172.20.10.5
Gateway：172.20.10.1
```

优先扫描：

```text
172.20.10.1:7890
172.20.10.1:1082
```

如果任意一个端口连接成功：

```text
直接认为找到代理
```

不需要继续扫描整个 `/24`。

如果网关的两个端口都不通，再执行完整局域网扫描。

------

# 六、电脑 A/B 直连场景

不要假设代理一定位于默认网关。

例如：

```text
A：192.168.10.10
B：192.168.10.20
```

B 没有配置默认网关，但：

```text
192.168.10.20:7890
```

开放。

程序仍然应该通过：

```text
192.168.10.0/24
```

扫描发现 B。

因此：

> 默认网关只是优先检测目标，不是唯一检测目标。

------

# 七、代理选择策略

如果扫描结果：

```text
192.168.10.20:7890  ✓
192.168.10.20:1082  ✓
192.168.10.30:7890  ✓
```

默认优先级：

```text
7890 > 1082
```

同端口存在多个 IP 时，可以优先选择 TCP 建连耗时最低的。

例如：

```text
192.168.10.20:7890  10ms
192.168.10.30:7890  35ms
```

选择：

```text
192.168.10.20:7890
```

注意：

> 这里的延迟只代表 TCP Connect 建连耗时，不代表代理实际访问互联网的速度。

------

# 八、Windows 系统代理

只修改当前用户的 Windows 系统代理。

注册表：

```text
HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings
```

设置：

```text
ProxyEnable = 1
ProxyServer = IP:Port
```

例如：

```text
ProxyEnable = 1
ProxyServer = 192.168.10.20:7890
```

关闭代理：

```text
ProxyEnable = 0
```

修改完成后，通过 Windows API 通知系统代理配置已经变化。

不使用 TUN。

不安装任何网络驱动。

------

# 九、网卡切换

电脑有：

- Ethernet
- Wi-Fi

管理要求：

> 有线和无线不能同时启用。

GUI 提供：

```text
[切换到有线]
[切换到 Wi-Fi]
```

例如当前：

```text
Ethernet：Enabled
Wi-Fi：Disabled
```

点击：

```text
切换到 Wi-Fi
```

执行：

```text
1. 禁用 Ethernet
2. 等待 Ethernet 状态变为 Disabled
3. 启用 Wi-Fi
4. 等待 Wi-Fi Connected
5. 获取 Wi-Fi IP
6. 获取 Wi-Fi 网段
7. 自动扫描 7890 / 1082
8. 找到端口后设置系统代理
```

反向切换同理。

------

# 十、网卡识别

不要根据中文名称判断。

通过 Windows 网络适配器属性识别：

```text
Ethernet
Wireless
```

GUI 显示：

```text
类型
名称
InterfaceDescription
状态
IPv4
```

例如：

```text
有线
Realtek PCIe GbE Family Controller
Enabled
192.168.10.10
无线
Intel(R) Wi-Fi 6E AX211
Disabled
-
```

------

# 十一、权限

程序尽量以普通用户运行。

普通功能：

- 获取网卡
- 获取 IP
- 获取网段
- 扫描 TCP 端口
- 设置当前用户系统代理

不要求管理员权限。

只有：

```text
启用 / 禁用网卡
```

需要时才请求 UAC。

不要默认整个程序都以管理员权限运行。

------

# 十二、自动模式

启动程序后：

```text
检测当前网卡
      ↓
获取 IP / CIDR
      ↓
优先扫描 Gateway
      ↓
没有找到
      ↓
扫描整个局域网
      ↓
找到 TCP 可连接端口
      ↓
自动设置系统代理
```

例如：

```text
发现：

172.20.10.1:7890

↓

设置：

HTTP Proxy
172.20.10.1:7890
```

------

# 十三、代理监控

程序需要定期检查当前代理。

例如：

```text
monitorInterval = 30
```

每 30 秒：

```text
TCP Connect 当前代理
```

如果连接成功：

```text
保持当前代理
```

如果连续失败：

```text
proxyCheckFailures = 3
```

则：

```text
当前代理失效
    ↓
重新扫描局域网
    ↓
找到新的 IP:Port
    ↓
切换系统代理
```

仍然只进行 TCP Connect。

**禁止通过代理访问 URL 进行检测。**

------

# 十四、网络变化检测

需要检测：

- 网卡启用/禁用
- 网卡连接状态变化
- IPv4 地址变化
- 网络切换

例如：

```text
Wi-Fi 断开
    ↓
重新连接
    ↓
IP变化
    ↓
重新获取 CIDR
    ↓
重新扫描代理
```

可以使用网络事件检测 + 定时轮询作为兜底。

------

# 十五、GUI

使用：

```text
Tkinter
```

不使用第三方 GUI 框架。

界面包含：

### 网络适配器

```text
┌────────────────────────────────────┐
│ 网络适配器                         │
│                                    │
│ ● Wi-Fi                            │
│   Intel Wi-Fi 6E AX211             │
│   IP: 172.20.10.5                  │
│   状态：已连接                     │
│                                    │
│ ○ Ethernet                         │
│   Realtek PCIe GbE                 │
│   IP: -                            │
│   状态：已禁用                     │
│                                    │
│ [切换到 Wi-Fi] [切换到有线]        │
└────────────────────────────────────┘
```

### 代理

```text
┌────────────────────────────────────┐
│ HTTP Proxy                         │
│                                    │
│ 当前代理：172.20.10.1:7890         │
│ 状态：TCP连接正常                  │
│                                    │
│ [扫描代理] [使用代理] [关闭代理]   │
└────────────────────────────────────┘
```

### 扫描结果

```text
IP              Port     Connect
172.20.10.1     7890     12ms
172.20.10.1     1082     15ms
```

注意：

> Connect 时间只是 TCP 建连时间，不代表 HTTP Proxy 实际速度。

### 日志

显示：

```text
12:30:01 当前网卡：Wi-Fi
12:30:01 IP：172.20.10.5
12:30:01 网段：172.20.10.0/24
12:30:01 优先检测 Gateway：172.20.10.1
12:30:01 172.20.10.1:7890 TCP连接成功
12:30:01 发现代理：172.20.10.1:7890
12:30:01 设置 Windows 系统代理成功
```

------

# 十六、配置文件

使用 JSON：

```json
{
  "ports": [7890, 1082],
  "preferPort": 7890,
  "scanTimeout": 500,
  "scanConcurrency": 100,
  "monitorInterval": 30,
  "proxyCheckFailures": 3,
  "autoScan": true,
  "autoSetProxy": true,
  "disableProxyWhenUnavailable": false
}
```

参数含义：

- `ports`：扫描端口
- `preferPort`：优先选择的端口
- `scanTimeout`：TCP Connect 超时时间，单位 ms
- `scanConcurrency`：并发扫描数量
- `monitorInterval`：代理监控间隔，单位秒
- `proxyCheckFailures`：连续失败多少次后认为代理失效
- `autoScan`：启动时自动扫描
- `autoSetProxy`：发现代理后自动设置
- `disableProxyWhenUnavailable`：找不到代理时是否关闭系统代理

------

# 十七、启动前代理状态

程序启动时保存当前 Windows 系统代理状态：

```text
ProxyEnable
ProxyServer
ProxyOverride
AutoConfigURL
```

程序运行过程中可以修改代理。

增加配置：

```json
{
  "restoreProxyOnExit": false
}
```

默认：

```text
false
```

程序退出时默认不恢复。

------

# 十八、代码结构

建议保持模块化：

```text
main.py

network.py
    网卡发现
    网卡状态
    IP / Mask / CIDR
    网卡启用/禁用

scanner.py
    局域网扫描
    TCP Connect
    并发控制

proxy.py
    Windows 系统代理读取
    设置
    关闭
    刷新 Windows 代理配置

monitor.py
    当前代理 TCP 状态监控
    网络变化监控
    自动重新扫描

config.py
    JSON 配置

gui.py
    Tkinter GUI
```

最终使用 PyInstaller 打包成：

```text
LANProxySwitcher.exe
```

目标电脑不需要安装 Python。

------

# 十九、线程模型

Tkinter 主线程不能被扫描阻塞。

需要：

```text
GUI 主线程
    │
    ├── 扫描线程
    │
    ├── 网卡切换线程
    │
    └── 监控线程
```

线程通过安全的队列/事件向 GUI 更新状态。

GUI 更新必须在 Tkinter 主线程执行。

禁止扫描整个 `/24` 时冻结 GUI。

------

# 二十、安全边界

只允许扫描：

> 当前本机已启用网络适配器对应的局域网 CIDR。

例如：

```text
192.168.10.0/24
```

不要扫描公网。

不要允许程序默认扫描任意用户输入的公网 IP 范围。