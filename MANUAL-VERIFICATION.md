# 手动验证清单

自动化测试覆盖不到的部分（真实网卡启停、真实系统代理、真实 GUI）逐条验证。
每次改动 network.py / proxy.py / gui.py / controller.py 后重跑本清单。

## 1. 网卡启用/禁用（Task 6）

- [ ] 列出网卡，各字段与「设置 → 网络和 Internet」显示一致
- [ ] 禁用一张空闲网卡，状态变为「已禁用」
- [ ] 重新启用，状态回到「未连接」或「已连接」
- [ ] 启动打包后的程序时 Windows 显示 UAC 提示；确认后可正常执行网卡启用/禁用

## 2. 系统代理（Task 7）

- [ ] `apply("127.0.0.1:7890")` 后，「设置 → 网络和 Internet → 代理」显示手动代理已开启且地址正确
- [ ] 浏览器新开标签页立即走代理（无需重启浏览器），验证 InternetSetOption 通知生效
- [ ] `disable()` 后设置页显示已关闭，但地址栏仍保留原地址
- [ ] 事先手动设一个 PAC 脚本地址，`apply()` 后 PAC 被清空且手动代理生效
- [ ] 程序日志首行的账户名/SID 与当前登录用户一致

（后续任务会往本文件追加第 3 节及以后的条目。）

## 3. 控制器与 GUI

- [ ] 启动后适配器表显示类型、名称、说明、状态、IPv4、网关和寻址方式；当前活动网卡带圆点
- [ ] 点击「扫描代理」时界面保持可操作，日志实时显示扫描进度和结果
- [ ] 选择一条扫描结果并点击「使用选中代理」后，代理状态显示该地址和 TCP 延迟
- [ ] 切换到另一张网卡时，日志按「禁用旧网卡 → 启用目标 → 获得 IPv4 → 扫描」顺序出现；DHCP 超时能给出明确日志
- [ ] 在代理启用期间断开并重连网络，网络稳定后会重新扫描；代理连续探测失败后也会重新扫描
- [ ] 关闭窗口后进程正常退出；`restoreProxyOnExit=true` 时启动前代理状态会被还原

## 4. 静态 / 动态 IP

- [ ] 表格的「寻址方式」列与「设置 → 网络和 Internet → 网卡 → IP 分配」一致；已禁用的网卡显示 `-`
- [ ] 未选中任何行时，「设为静态 IP」「设为动态 IP」两个按钮为灰
- [ ] 选中一张空闲网卡，弹窗预填其当前 IP / 前缀 / 网关；应用后 Windows 设置页显示手动 IP 分配且地址正确
- [ ] 应用成功后 `config.json` 的 `staticIpProfiles` 出现该网卡名，再次打开弹窗按存档预填
- [ ] IP 或前缀填错时只在日志区报错，界面不弹模态框、不卡死
- [ ] 点「设为动态 IP」后设置页 IP 分配变回「自动 (DHCP)」，DNS 也回到自动获取
- [ ] 上述两步完成后日志都会接着出现一轮新的代理扫描
- [ ] 静态 IP 填成不通的网段时，仍能用「设为动态 IP」把网卡救回来

## 5. 设置弹窗

- [ ] 右上角「设置」打开弹窗，各输入框与 `config.json` 当前内容一致
- [ ] 把 TCP 超时改成 `abc`，点「保存」后弹窗不关、红字提示「TCP 超时」，`config.json` 未被改动
- [ ] 把首选端口改成不在扫描端口列表里的值，保存被拒且提示指出这一点
- [ ] 改一项合法值并保存：弹窗关闭，日志出现「配置已保存并生效」，`config.json` 同步更新
- [ ] 保存新的扫描端口后点「扫描代理」，日志显示按新端口探测（无需重启）
- [ ] 把监控间隔改短并保存，代理健康探测按新间隔出现在日志里（无需重启）
- [ ] 先用 IP 弹窗存一个静态档位，再从设置弹窗保存一次，`config.json` 的 `staticIpProfiles` 仍在
- [ ] 「打开所在目录」在资源管理器里定位到 `config.json` 所在文件夹
